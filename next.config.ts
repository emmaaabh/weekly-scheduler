import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    "3000--dev--weekly-scheduler--emmab--apps.us-east-2.cdrstable.dev",
    "https://3000--dev--weekly-scheduler--emmab--apps.us-east-2.cdrstable.dev",
  ],
};

export default nextConfig;
